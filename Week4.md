
# MATLAB 4주차

```
.* (안에다 곱하기)
.^ (안에다 ^)

Ax = b 에서 x를 구하는 3가지 방법
x = a^-1*b
x = inv(A)*b
x = A\b

3x3 * 3x1 = 3x1 식을
1x3 * 3x3 = 1x3 형식으로 바꿔도 됨

mean(A) -> 평균
C = max(A)
[d,n] = max(x) 숫자, 인덱스
min(A)
[d,n] = min(x) 숫자,인덱스
sum(A) -> 합

sort(A) -> 정렬 (낮은 수 부터)
median(A) -> 중앙값
std(A)
det(A)
dot(a,b) -> dot product = sum(a.*b) , = a * b'
cross(a,b) -> cross product
inv(A) -> inverse matrix

rand
rand(n)
rand(m,n)
rand(1,n)
```
